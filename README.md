
# Digital Steganography in Java

A simple yet effective Java-based application that hides secret messages inside images using digital steganography techniques.

##  Features

- Embed secret text into an image file (PNG/BMP)
-  Decode hidden messages from images
- Support for multiple image formats (lossless)
- GUI-based interface (Java Swing)
- Preserves image quality and size
